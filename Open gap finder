# 1. Build the package to register your changes
colcon build --packages-select my_package

# 2. Source the Global ROS environment
source /opt/ros/foxy/setup.bash

# 3. Source your local workspace so it knows about the new build
source install/local_setup.bash

# 4. Run your script
python3 src/my_package/my_package/gap_finder_base.py
